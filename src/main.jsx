import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Renderiza o componente principal <App /> no elemento com o id 'app' no HTML.
ReactDOM.render(<App />, document.getElementById('app'));
